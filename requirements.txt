# Core runtime dependencies used directly by the project source code.
numpy==1.22.4
openai==1.75.0
pycld3==0.22
scikit-learn==1.0.2
sentence-transformers==4.1.0
torch==2.0.1
tqdm==4.67.1
transformers==4.49.0

# FAISS backend used by the retrieval code. Replace with `faiss-cpu==1.7.2`
# if you need a CPU-only environment.
faiss-gpu==1.7.2
